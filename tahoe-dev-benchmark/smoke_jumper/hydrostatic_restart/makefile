# Make file to clean directory of Tahoe files

ifdef COMSPEC
  SUFFIX = .exe
  RM = del
else
  SUFFIX =
  RM = rm -f
endif

clean:
	$(RM) *.io0.*
	$(RM) *.io1.*
	$(RM) *.echo.xml
	$(RM) *.valid.xml
	$(RM) hydrostatic.rs1000of1000*
	$(RM) *.out




