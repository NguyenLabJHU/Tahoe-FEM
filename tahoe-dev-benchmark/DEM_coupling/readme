1. What data is needed to run de-fe coupling?
   fem.xml (tahoe_DEM_coupling)
   dem.dat

2. How to run?
   tahoe -f fem.xml 

3. How to compile?

   a). To apply constant forces to free particles, in assembly::ReadSample():
       uncomment line 460: pt->setConstForce(vec(fx,fy,fz));

   b). If constant forces are supposed to apply gradually on free particles,
       in assembly::Run():
       comment line 2809:   setForceZero();
       uncomment line 2810: setForceZero(PrintNum);

4.How to postprocess?
  plot dem_particle 0 99 1 (generate de dat)
  mycat (combine de and fe dat)
  mypre (convert to binary)
  tecplot &
